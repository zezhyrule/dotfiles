#include<stdio.h> // library needed for simple I/O

int square(int x);

int main() // create a main function with return type int
{          // return 0 if successful. C standard is to have a return type.

	printf("%d", square(3)); // standard print func display to console
	return 0;
} // end main

int square(int x)
{
	return x * x;
}
